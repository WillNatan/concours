{% extends 'base.html.twig' %}

{% block title %}Edit FormContest{% endblock %}

{% block body %}
    <h1>Edit FormContest</h1>

    {{ include('form_contest/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('form_contest_index') }}">back to list</a>

    {{ include('form_contest/_delete_form.html.twig') }}
{% endblock %}
